One file per hand-written component (e.g. `retry-policy.md`), using
`common/templates/lld.md` as the shape. Write this ALONGSIDE the actual
code in `@lab/lld` — the doc records the design decision, the code is the
implementation.
