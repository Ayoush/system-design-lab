Write `v0.md` here first, using `common/templates/hld.md` as the shape.
One file per version — `v0.md`, `v1.md`, `v2.md`... each one records what
changed and why, not the whole architecture from scratch every time.
