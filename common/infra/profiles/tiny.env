# TINY — breaks around ~500 RPS instead of nano's ~150 RPS.
# Same file shape as nano.env — only the numbers change.

API_CPUS=0.5
API_MEM=512m
PG_CPUS=1.0
PG_MEM=512m
PG_MAX_CONNECTIONS=50
PG_SHARED_BUFFERS=32MB
PG_WORK_MEM=256kB
APP_POOL_SIZE=10
PGBOUNCER_POOL_SIZE=10
REDIS_MAXMEM=32mb
